# 포트폴리오 사이트 개선안

> 작성일: 2026-05-05
> 기반: 받은 피드백 + 20문항 수요조사 답변

---

## 1. 받은 피드백

1. 너무 **난잡**하다
2. 짧은 시간 안에 **"이런 사람이구나" 느낌**이 와야 한다
3. **텍스트가 눈에 안 들어온다**
4. 페르소나(설계 · AI · 검증)와 프로젝트 증거가 **자동으로 이어지지 않는다** — 보는 사람이 "그래서 그게 어디 있는데?"가 됨

이 네 개를 **정면으로** 푸는 게 이번 개선의 목적.

---

## 2. 타겟·기준

| 항목 | 결정 |
|---|---|
| 1차 타겟 | 기업 리크루터/인사담당자 (비기술 가능성 큼) |
| 첫인상 시간 budget | **5–10초** (Hero만 보고도 정체성 전달) |
| 1차 디바이스 | 데스크톱 우선 |
| 다크/라이트 | 둘 다 유지 |

---

## 3. 핵심 원칙 (모든 결정의 근거)

1. **5–10초 안에 "Java·Spring 백엔드 신입"이 보일 것** → 메인 헤드라인은 기술 스택 중심
2. **페르소나는 부제, 증거는 프로젝트 칩으로** → "설계 · AI · 검증" 부제 + 각 프로젝트 카드 상단에 동일 키워드 칩으로 매핑
3. **본문 가독성 = 산세리프** → 모노스페이스는 코드·스택·터미널 안에만
4. **섹션 줄이기** → Hero / Projects / Contact 중심. Skills 단독 섹션 제거, About 압축

---

## 4. 섹션별 개선안

### A. Hero (가장 중요한 변화)

```
┌─────────────────────────────────────────────┐
│  백종현                                      │
│  Java · Spring 백엔드 신입                   │  ← 메인 헤드라인
│                                              │
│  설계 · AI · 검증                            │  ← 페르소나 부제
│                                              │
│  [Java] [Spring] [Kafka] [Redis] [AWS] ...  │  ← 핵심 스택 칩
│                                              │
│  [📄 이력서 PDF]   [GitHub]                  │  ← CTA (이력서가 1순위)
└─────────────────────────────────────────────┘
```

- 이름 + 직군 = 메인 헤드라인 (지금의 추상적 tagline은 메인에서 빼기)
- 페르소나는 작은 부제로 (3 키워드만)
- 핵심 스택 칩 4–6개 (Skills 섹션을 흡수)
- 사진 없음
- CTA 1순위 = 이력서 PDF 다운로드. 2순위 = GitHub

### B. About

- 현재 4카드(whoami · education · bootcamp · activity) → **1–2 문단**으로 압축
- 압축 후 들어갈 내용:
  - 자기소개 1줄 (현재 introduction의 첫 문장 정도)
  - 학력 + 부트캠프 1줄
  - 핵심 관심사 / 강점 1줄

### C. Projects (가장 큰 콘텐츠 변화)

**프로젝트 수**: 1–2개 핵심만 — 현재 2개(CineStream, 동네마켓) 그대로 유지.

**각 프로젝트 카드 상단에 페르소나 칩 추가**:

```
┌─────────────────────────────────────────┐
│  [설계] [AI] [검증]   ← 페르소나 칩 (신규)│
│  CineStream                              │
│  영화 티켓팅 + 라이브 스트리밍 OTT...     │
└─────────────────────────────────────────┘
```

**각 프로젝트 안에 'AI 활용' 서브섹션 신설** — 두 단계로 표시:

- **카드용 한 줄 요약** = 5–10초 리크루터 시야에 들어옴
- **모달 상세** = 관심 있는 사람(기술 리크루터·팀장)이 펼쳤을 때 차별화 포인트 노출

#### 동네마켓 — AI 활용

**카드 한 줄**:
> 문서 기반으로 작업을 스프린트 단위로 나누어 AI에 위임하고, 산출물을 직접 테스트로 검증.

**모달 상세**:
> 설계 문서를 기반으로 스프린트 단위 작업을 분할해 AI에 위임. 산출물은 본인이 직접 테스트로 검증. 작업 성격에 따라 모델 분담 — 설계는 Opus, 구현은 Sonnet, 문서는 Haiku. 중요한 작업 전엔 컨텍스트 용량을 점검해 압축·초기화하고, 주기적으로 `/init`을 돌려 프로젝트 문서를 최신 상태로 유지.

#### CineStream — AI 활용

**카드 한 줄**:
> 문서 기반으로 스프린트별 작업과 보고서 생성을 AI에 지시. 보고서로 1차 검증 후 스프린트별 통합테스트로 최종 확인.

**모달 상세**:
> 설계 문서를 기반으로 스프린트별 작업과 작업 보고서 생성을 AI에 함께 지시. 보고서를 1차 검증한 뒤 스프린트별 통합테스트로 마감. 모델 분담 동일 — 설계 Opus / 코딩 Sonnet / 문서 Haiku. 컨텍스트 관리 루틴(점검 → 압축·초기화 → `/init`)도 동일.

> **공통 워크플로우 요약** (모달 또는 별도 섹션에서 한 번만 명시 가능):
> - **백엔드**: 작업 성격별 모델 분담 (설계 Opus / 코딩 Sonnet / 문서 Haiku)
> - **프론트엔드**: Google Antigravity + `agentation`(클릭 기반 dev 피드백 수집 — 이 사이트도 동일 도구로 개발)
> - **컨텍스트 운영**: 중요 작업 전 토큰 사용량 점검 → 압축 또는 초기화
> - **출력 양식 통일**: AI 출력 스타일을 별도 지정해 산출 문서 포맷을 일관화
> - **문서 최신화**: 주기적 `/init`으로 프로젝트 문서(CLAUDE.md 등) 갱신

→ 이 워크플로우가 페르소나 3축을 한 흐름에 다 넣음:
- **설계** = 문서 기반 작업 분할
- **AI** = 스프린트 단위 위임 + 모델 분담
- **검증** = 직접 테스트 / 보고서 검증 / 통합테스트

**기존 트러블슈팅 PSR 블록은 유지**. 이미 "검증" 축의 가장 강한 증거.

### D. Skills

- 단독 섹션 **제거**.
- 핵심 4–6개는 Hero 스택 칩으로.
- 자세한 스택 정보는 각 프로젝트의 `stack` 배열에 이미 있음 → 그대로.

### E. TerminalWidget

- **제거**. 같은 개발자에겐 호감이지만, 비기술 리크루터에겐 노이즈/장난스러움. "난잡함" 피드백의 직접 원인일 가능성 큼.
- 백틱 키 리스너, `>_` 버튼, validTargets 등 관련 코드 모두 정리.

### F. 시각·타이포

| 항목 | 현재 추정 | 변경 |
|---|---|---|
| 본문 폰트 | 모노스페이스 | **Pretendard 등 산세리프** |
| 코드·스택·터미널 폰트 | 모노스페이스 | 그대로 모노 유지 |
| 강조 표시 | 보통 | **최소** (제목·핵심 단어만) |
| 액센트 컬러 | 다채로움 추정 | **단일 액센트 색** |
| 애니메이션 | 다수 | **줄이기** — `whileInView` 페이드만, 패럴랙스/플로팅 효과 제거 |

→ "텍스트가 눈에 안 들어온다" 피드백의 정면 해결.

---

## 5. profile.js 재작성 가이드

### 현재 → 변경

| 필드 | 현재 | 변경 방향 |
|---|---|---|
| `title` | `'신입 백엔드 개발자'` | `'Java · Spring 백엔드 신입'` (스택 키워드 포함) |
| `tagline` | `'설계로 소통하고, AI로 가속하며, 검증으로 완성하는 개발자.'` | **부제로 강등**: `'설계 · AI · 검증'` (3 키워드만) |
| `introduction` | 2 문단 약 600자 | About 압축에 맞춰 **1 문단 200–250자**. "신뢰하되 검증한다(Trust, but Verify)" 원칙은 유지 — 페르소나 "검증" 축의 핵심 표현 |
| `strengths` | 8개 | 그대로 유지 (Skills 섹션이 사라지므로 어딘가엔 있어야 함; About 또는 Hero 부속으로) |

### "AI로 가속" 부분 introduction 다듬기 제안

> **현재**: "최근에는 AI 코딩 도구를 개발 프로세스에 적극 도입해 생산성을 높이고 있습니다. 다만 AI의 결과물을 맹목적으로 수용하지는 않습니다…"
>
> **다듬은안**: "설계 문서를 기반으로 작업을 스프린트 단위로 나누어 AI에 위임하고, 산출물을 보고서·통합테스트로 검증하는 사이클을 실제 프로젝트(CineStream, 동네마켓)에 적용해 왔습니다."

→ **추상→구체** 전환. 5–10초 리크루터에게 "이 사람이 뭘 하는지" 즉시 전달.

---

## 6. 변경할 파일 목록

| 파일 | 작업 |
|---|---|
| `src/data/profile.js` | `title`·`tagline`·`introduction` 재작성. `strengths` 그대로 |
| `src/data/projects.js` | 각 프로젝트에 `personaTags: ['설계','AI','검증']` 필드 추가, `aiWorkflow` 필드 신설 (위 초안 두 줄) |
| `src/components/Hero.jsx` | 새 헤드라인 구성, CTA 단순화(이력서 PDF + GitHub 둘만), 스택 칩 통합 |
| `src/components/About.jsx` | 4 카드 → 1–2 문단으로 단순화. `aboutItems` 배열 제거 |
| `src/components/Projects.jsx` 또는 `ProjectCard` 부분 | `personaTags` 칩 렌더 추가 |
| `src/components/ProjectDetail.jsx` | `aiWorkflow` 서브섹션 렌더 추가 (PSR 블록 위쪽 권장) |
| `src/App.jsx` | `<Skills>` 제거, `<TerminalWidget>` 제거, 관련 import 정리 |
| `src/components/Skills.jsx` · `src/components/TerminalWidget.jsx` | 파일 삭제 |
| `src/data/skills.js` | 파일 삭제 (또는 strengths만 남기고 정리) |
| `src/styles/tokens.css` 또는 `globals.css` | 본문 `font-family`를 산세리프(Pretendard)로 변경. 액센트 컬러 단일화 |
| `index.html` | Pretendard 등 한글 산세리프 웹폰트 link 추가 |
| `CLAUDE.md` | TerminalWidget·Skills 관련 항목 제거. `validTargets` 항목 갱신 |

---

## 7. 실행 순서 (작은 PR 단위)

1. **콘텐츠 먼저**: `profile.js` · `projects.js` 데이터 갱신 (페르소나 칩 + AI 활용 필드)
2. **Hero 재구성**: 새 헤드라인 / CTA / 스택 칩
3. **삭제**: `Skills`, `TerminalWidget` 제거 (App.jsx import 포함)
4. **About 압축**: 4카드 → 문단
5. **ProjectDetail에 AI 활용 섹션 + 페르소나 칩 렌더 추가**
6. **타이포·컬러 다듬기**: 본문 산세리프 전환, 액센트 컬러 단일화, 애니메이션 축소
7. **로컬 검증**: `npm run dev`로 라이트/다크 둘 다 확인, 데스크톱 1440·1920에서 Hero 5초 룩 확인

---

## 8. 자가검증 체크리스트 (배포 전)

- [x] **5초 테스트**: 처음 보는 사람에게 5초 보여주고 "이 사람 직군이 뭐야?" 물었을 때 "Java/Spring 백엔드"가 나오는가
- [x] **페르소나 흔적**: Hero 부제(설계 · AI · 검증)와 프로젝트 칩(설계 · AI · 검증)이 시각적으로 같은 키워드로 보이는가
- [x] **AI 활용란**: 두 프로젝트 모두 "AI 활용" 슬라이드가 있고, 공통 워크플로우 구조화 표시
- [x] **본문 가독성**: 본문 Pretendard 전환, ProjectCard·TroubleshootingSlide 본문 산세리프, 한글 자간 확인
- [x] **삭제 확인**: TerminalWidget · Skills · Resume 단독 섹션 제거 완료
- [x] **CTA**: Hero에 이력서 PDF 다운로드 버튼 추가 (파일은 사용자가 `public/resume.pdf` 배치)
- [ ] **placeholder**: `[채워주세요]`나 `[실제 측정값]`이 노출되지 않는가 — 배포 직전 본인 확인

---

## 9. 안 한 것 (의도적)

- 측정값 채우지 않음 — `troubleshooting.result`의 측정값은 본인이 직접 측정해 채울 영역. CLAUDE.md 규칙대로 추정값 금지.
- 사진·아바타 추가 안 함 — 미니멀 방향과 충돌. 이력서 PDF 안에는 사진 넣어도 무방.
- 모바일 전용 최적화 깊게 안 함 — 데스크톱 우선 결정. 깨지지 않을 정도의 반응형만.
- 블로그/타임라인 신설 안 함 — 5–10초 타겟과 충돌.
